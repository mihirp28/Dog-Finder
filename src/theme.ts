// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      // This sets the default background color for the entire app
      default: '#f7f7f5', // or whatever off-white color you want
    },
  },
});

export default theme;
